{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "storybook": "lerna run --scope @anya-ui/components storybook",
    "demo": "lerna run --scope @anya-ui/demo start"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
